@inject NavigationManager NavManager

<AuthorizingInfo />

@code {
    protected override void OnInitialized()
    {
        NavManager.NavigateTo($"/api/Account/Login?returnUrl={NavManager.Uri}", true, true);
    }
}