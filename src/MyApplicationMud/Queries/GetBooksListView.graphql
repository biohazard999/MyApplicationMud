query GetBooksListView {
	items: books {
		...BookListInfo
	}
}

subscription BooksSubscription {
	changed: bookChanged {
		...BookListInfo		
	}  
}

fragment BookListInfo on Book {
	id
	title,
	image,
	author {
		name
	}
}
