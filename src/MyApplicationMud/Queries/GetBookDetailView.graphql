query GetBooksDetailView($id: Int!) {
	details: book(id: $id) {
		id
		title
		image
		author {
			...AuthorInfo
		}
	}
	authors: authors {
		...AuthorInfo
	}
}

fragment AuthorInfo on Author {
	id,
	name
}